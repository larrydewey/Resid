## Communication
When caveman mode is active, apply wenyan-style compression: omit subjects, use verb-first order where natural in English, drop all particles/conjunctions, state cause→effect in minimal words. Output language: English.   

- **Status**: 821 tests pass (lexer 17, parser 122, resid-ir 59, resid-type 260, resid-codegen 137, resid-build 47, resid-fmt 5, resid-cache 17, resid-notes 3, resid-why 8, resid-lsp 1, resid-lsp-notes 6, resid-graph 4, resid-diag 6, residc 0 unit + 129 e2e). Full workspace suite ≈ 85 minutes wall-clock (`cargo nextest run --workspace`, 31 tests classified "slow" i.e. ≥60s — live-network HTTP/2+TLS, wide-EC crypto property tests, and bootstrap-driver/parity runs; none of these are hangs, they're genuinely slow work). See the table below before running the full suite blind; for iteration, filter to what you're touching: `cargo test -p residc --test e2e -- <filter>` or `cargo nextest run -E 'test(<filter>)'`. Frontend → LLVM → native binaries all working; **stage-2 self-hosting proven, including full sandbox/capability parity** — `examples/driver.resid` (bootstrap driver regenerated by `tools/merge_driver.py` from `examples/typecheck.resid` + `examples/codegen.resid`) compiles programs identically to the Rust pipeline, including sandbox enforcement (E0211/E0212/E0213, handle-entry, value-provenance, read-only modes) and the `resid_cap_enter`/`resid_cap_check`/`resid_cap_leave` force-time guard (e2e: `bootstrap_*` tests, including `bootstrap_driver_sandbox_*`).

## Slowest tests (≥60s), most recent full run

Wall time sums to ~85 min because the workspace's slow tests (live network I/O,
LLVM codegen + clang link + native execution for every `bootstrap_*`/`run_*`
case, and O(2^N)-ish crypto property tests) dominate; the other ~790 tests
finish in well under a second each. Use this table to decide what to skip
during iteration and what to expect when running the full suite.

| Test | Duration |
|---|---|
| `run_ecge512_wide_prop_in_resid` | 10m 51.5s |
| `run_ecdsa_prop_in_resid` | 10m 36.1s |
| `bootstrap_typechecker_accepts_bootstrap_sources` | 4m 11.7s |
| `run_h2_hpack_in_resid` | 3m 52.2s |
| `bootstrap_driver_sandbox_handle_entry` | 3m 06.9s |
| `run_else_if_chain` | 3m 00.2s |
| `bootstrap_map_set_parity` | 2m 58.4s |
| `bootstrap_driver_sandbox_transitive_attenuation` | 2m 56.1s |
| `bootstrap_behavior_ord_parity` | 2m 44.8s |
| `run_rsa_pkcs1_verify_in_resid` | 2m 41.6s |
| `bootstrap_driver_sandbox_readonly_mode` | 2m 40.6s |
| `run_h2_post_and_continuation_in_resid` | 2m 16.3s |
| `bootstrap_graph_reduce_parity` | 1m 59.3s |
| `bootstrap_graph_reduce_eliminates_dead_bindings` | 1m 57.1s |
| `bootstrap_driver_sandbox_enforcement` | 1m 51.2s |
| `bootstrap_driver_fixed_size_stack_types` | 1m 50.2s |
| `bootstrap_driver_compiles_and_rejects` | 1m 49.6s |
| `bootstrap_driver_fixed_builtin_widening` | 1m 47.2s |
| `bootstrap_driver_sandbox_force_time_guard` | 1m 40.0s |
| `run_crypto_kit` | 1m 24.8s |
| `run_tls13_live_openssl_in_resid` | 1m 15.9s |
| `run_x509_in_resid` | 1m 08.1s |
| `bootstrap_question_else_parity` | 1m 03.1s |
| `run_wide_int_boxing` | 1m 02.5s |
| `bootstrap_match_parity` | 1m 01.3s |
| `run_tcp_externs_both_pipelines` | 1m 01.0s |
| `run_h2_live_request_in_resid` | 1m 00.8s |
| `bootstrap_option_sum_parity` | 1m 00.5s |
| `run_stage2_import_resolution` | 1m 00.1s |
| `bootstrap_graph_reduce_rejects_declarations` | 1m 00.1s |
| `run_str_builder_in_resid` | 1m 00.0s |

All 31 are in `crates/residc/tests/e2e.rs`. Everything else in the workspace
(the other ~790 tests) finishes in well under a second.
