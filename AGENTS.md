## Communication
When caveman mode is active, apply wenyan-style compression: omit subjects, use verb-first order where natural in English, drop all particles/conjunctions, state cause→effect in minimal words. Output language: English.   

- **Status**: The compiler is self-hosted and cargo-free: `./boot.sh` links the committed seed (`build/boot/seed.ll`) and must reach a byte-identical self-compile fixed point. Its pipeline runs entirely on the knowledge graph (spec §3, `PLAN-graph-ir.md` G0–G7): parse (`examples/graph.resid`), resolve (`resolve.resid`), check (`gcheck.resid`), reduce (`greduce.resid`), lower (`lower.resid`), and the graph artifact (`gart.resid`). The text checker, reducer, codegen and notes scan are retired. The source is read after parsing only for diagnostic text and source-map lines. `examples/driver.resid` is generated by `tools/merge_driver.py` from `examples/codegen.resid` (the LLVM emission layer) and `examples/typecheck.resid` (the type algebra), followed by the hand-written driver tail. Self-hosted suites: `tests/conformance/run.sh` (161), `tests/reduce/run.sh` (14), `tests/provenance/run.sh` (20) and `tests/graph/run.sh` (390, including resid-why, resid-graph and resid-debug, live through its own ptrace backend and, when installed, gdb). The archived Rust workspace under `bootstrap/rust-stage0/` still has its `cargo test` suites; its e2e tests that ran the retired standalone checker and codegen, or `--bootstrap-graph-reduce`, were removed. The self-compile takes about 2s at a 370MB peak (release) and 519MB (debug); run it under `ulimit -v`.

## Slowest tests (≥60s), most recent full run

**Stale as of 2026-09-19 for every `bootstrap_*` row**: those all run the
self-hosted driver, and a fixed O(N^2) `str_len` bug in its runtime made
every one of them dramatically faster. Left as
historical baseline plus the still-accurate live-network/crypto-property
rows (`run_ec*`, `run_rsa*`, `run_h2*`, `run_tls*`, `run_crypto_kit`,
`run_x509*`, `run_tcp*`), which are Rust-pipeline/network-bound and
unaffected by that fix. Not re-measured row-by-row post-fix (the suite ran
in about 16.5 minutes after it).

Wall time sums to ~85 min because the workspace's slow tests (live network I/O,
LLVM codegen + clang link + native execution for every `bootstrap_*`/`run_*`
case, and O(2^N)-ish crypto property tests) dominate; the other ~790 tests
finish in well under a second each. Use this table to decide what to skip
during iteration and what to expect when running the full suite.

| Test | Duration |
|---|---|
| `run_ecge512_wide_prop_in_resid` | 10m 51.5s |
| `run_ecdsa_prop_in_resid` | 10m 36.1s |
| `run_h2_hpack_in_resid` | 3m 52.2s |
| `bootstrap_driver_sandbox_handle_entry` | 3m 06.9s |
| `run_else_if_chain` | 3m 00.2s |
| `bootstrap_map_set_parity` | 2m 58.4s |
| `bootstrap_driver_sandbox_transitive_attenuation` | 2m 56.1s |
| `bootstrap_behavior_ord_parity` | 2m 44.8s |
| `run_rsa_pkcs1_verify_in_resid` | 2m 41.6s |
| `bootstrap_driver_sandbox_readonly_mode` | 2m 40.6s |
| `run_h2_post_and_continuation_in_resid` | 2m 16.3s |
| `bootstrap_driver_sandbox_enforcement` | 1m 51.2s |
| `bootstrap_driver_fixed_size_stack_types` | 1m 50.2s |
| `bootstrap_driver_compiles_and_rejects` | 1m 49.6s |
| `bootstrap_driver_fixed_builtin_widening` | 1m 47.2s |
| `bootstrap_driver_sandbox_force_time_guard` | 1m 40.0s |
| `run_crypto_kit` | 1m 24.8s |
| `run_tls13_live_openssl_in_resid` | 1m 15.9s |
| `run_x509_in_resid` | 1m 08.1s |
| `bootstrap_question_else_parity` | 1m 03.1s |
| `run_wide_int_boxing` | 1m 02.5s |
| `bootstrap_match_parity` | 1m 01.3s |
| `run_tcp_externs_both_pipelines` | 1m 01.0s |
| `run_h2_live_request_in_resid` | 1m 00.8s |
| `bootstrap_option_sum_parity` | 1m 00.5s |
| `run_stage2_import_resolution` | 1m 00.1s |
| `run_str_builder_in_resid` | 1m 00.0s |

All 27 are in `bootstrap/rust-stage0/crates/residc/tests/e2e.rs` (the Rust
pipeline is archived under `bootstrap/rust-stage0/` as of Phase D — see
`PLAN-resid-only.md`). Everything else in the workspace
(the other ~790 tests) finishes in well under a second.
