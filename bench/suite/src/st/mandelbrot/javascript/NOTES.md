# st / mandelbrot / javascript

Written for this suite. It follows the Benchmarks Game description of mandelbrot, single-threaded (node, no worker_threads).

Algorithm: 50 iterations, escape when Tr+Ti > 4, 8 pixels per byte, binary PBM.

No deviations from the reference algorithm.
