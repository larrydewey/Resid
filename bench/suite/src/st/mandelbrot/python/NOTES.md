# st / mandelbrot / python

Written for this suite. It follows the Benchmarks Game description of mandelbrot, single-threaded (CPython, stdlib only, no multiprocessing or native extensions).

Algorithm: 50 iterations, escape when Tr+Ti > 4, 8 pixels per byte, binary PBM.

No deviations from the reference algorithm.
