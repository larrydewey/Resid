# st / nbody / python

Written for this suite. It follows the Benchmarks Game description of nbody, single-threaded (CPython, stdlib only, no multiprocessing or native extensions).

Algorithm: Plain double-precision loop over body pairs, dt=0.01.

No deviations from the reference algorithm.
