# fannkuch-redux / fortran / st

Own implementation of the reference algorithm (count-array permutation order, alternating-sign checksum).

Single-threaded, no SIMD intrinsics. Build: `gfortran -O2` (GCC 16.2.1).
