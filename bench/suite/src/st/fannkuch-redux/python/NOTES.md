# st / fannkuch-redux / python

Written for this suite. It follows the Benchmarks Game description of fannkuch-redux, single-threaded (CPython, stdlib only, no multiprocessing or native extensions).

Algorithm: Permutation generation in the reference C order, with prefix flips and an alternating-sign checksum.

No deviations from the reference algorithm.
