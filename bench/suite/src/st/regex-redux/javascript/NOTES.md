# st / regex-redux / javascript

Written for this suite. It follows the Benchmarks Game description of regex-redux, single-threaded (node, no worker_threads).

Algorithm: Strips headers and newlines, counts 9 variant patterns, then applies the 5 IUB substitutions in order. Regex: built-in RegExp.

No deviations from the reference algorithm.
