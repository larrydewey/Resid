# st / binary-trees / javascript

Written for this suite. It follows the Benchmarks Game description of binary-trees, single-threaded (node, no worker_threads).

Algorithm: Allocates full binary trees of node objects and recursively counts their nodes. No pooling.

No deviations from the reference algorithm.
