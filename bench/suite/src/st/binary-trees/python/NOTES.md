# st / binary-trees / python

Written for this suite. It follows the Benchmarks Game description of binary-trees, single-threaded (CPython, stdlib only, no multiprocessing or native extensions).

Algorithm: Allocates full binary trees of node objects and recursively counts their nodes. No pooling.

No deviations from the reference algorithm.
