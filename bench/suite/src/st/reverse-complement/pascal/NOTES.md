# reverse-complement / pascal / st

Own implementation of the reference algorithm. Stdin is read whole with `FileRead`, output goes out with `FileWrite`. Ignores argv.

Free Pascal 3.2.2 stands in for Delphi/Object Pascal. Single-threaded, no SIMD intrinsics. Build: `fpc -O2`.
