# pidigits / pascal / st

Own implementation of the reference spigot algorithm, using FPC's bundled `gmp` unit (thin bindings to GMP's mpz API; links libgmp).

Free Pascal 3.2.2 stands in for Delphi/Object Pascal. Single-threaded, no SIMD intrinsics. Build: `fpc -O2`.
