Int main() {
    Int(128) big = (Int(128))1000000 * 1000000;
    Option(Int(128)) o = Some(big);
    Int(128) got = match o { Some(v) => v, None => i128(0) };
    if (got == big) { println("I128:PASS"); } else { println("I128:FAIL"); }
    Str is = f"{got}";
    println(is);
    return 0;
}
