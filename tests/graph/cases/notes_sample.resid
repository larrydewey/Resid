// Notes come from the graph: this comment's rt and args.count() are text.
Int main() {
    Int a = rt(0 - 1);
    Int n = args.count();
    Str s = "rt args.count()";
    println(IntToString(a + n) + s);
    return 0;
}
